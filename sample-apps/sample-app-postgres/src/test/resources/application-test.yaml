spring:
  datasource:
    # When running locally test containers are used
    # When running in CI an external service is used
    username: runner
    password: runner
    url: jdbc:postgresql://127.0.0.1:5432/integration-test-db
